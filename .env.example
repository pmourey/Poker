# Exemple de configuration d'environnement pour l'application Poker
# Copiez ce fichier en .env puis ajustez les valeurs pour votre environnement local
# Les variables ci-dessous sont lues par python-dotenv via app.py et start_server.py

# Clé secrète Flask (changez en production)
SECRET_KEY=change-me-in-prod

# Réseau serveur
HOST=0.0.0.0
PORT=5000

# Mode async Socket.IO (threading par défaut; alternatives: eventlet, gevent)
SOCKETIO_ASYNC_MODE=threading

# Délai (secondes) avant d'enchaîner automatiquement la main suivante
NEXT_HAND_DELAY_SECONDS=4

